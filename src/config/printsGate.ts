/** Flip true when prints economy ships — unlocks real shop purchases. */
export const PRINTS_ECONOMY_LIVE = false

/** Dev preview storefront (teaser when prints are off). */
export const COSMETICS_SHOP_ENABLED = true
