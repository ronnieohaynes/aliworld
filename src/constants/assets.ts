/** Public folder paths (Vite serves `public/` at site root). */
export const BATTLE_BACKGROUND_PATH = '/ALIWORLD EPISODE 1 BACKGROUND.png'

/** Safe URL for `<img>` / canvas `src` (spaces encoded). */
export const BATTLE_BACKGROUND_SRC = encodeURI(BATTLE_BACKGROUND_PATH)

/** CSS pixels per logical “superpixel” block in the battle background canvas. */
export const BATTLE_BG_PIXEL_BLOCK_PX = 5
